import { DiscType } from "../disc/disc";

export class Player {
    firstName: string;
    discType: DiscType;

    constructor() { }

    public initializePlayerFromJSON(jsonData: any): void {

    }
}